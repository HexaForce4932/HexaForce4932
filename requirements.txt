numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
PyYAML>=6.0
joblib>=1.3
requests>=2.31
pytest>=7.4

# Optional, install on the target hardware as needed:
# sounddevice>=0.4.6   # live microphone capture (pip install .[live])
# RPi.GPIO>=0.7.1      # GPIO LED alerts on a Raspberry Pi (pip install .[pi])
